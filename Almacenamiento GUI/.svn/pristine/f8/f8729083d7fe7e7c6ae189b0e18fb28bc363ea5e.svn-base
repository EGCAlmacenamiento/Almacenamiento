package services;

import java.util.ArrayList;

import org.apache.log4j.Category;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;

import utils.AbstractTest;
import domain.Item;
import domain.ItemQuantityCart;
import domain.ItemStockInWarehouse;
import domain.Money;
import domain.ShoppingCart;
import domain.Tax;

@RunWith(SpringJUnit4ClassRunner.class)
@ContextConfiguration(locations= { "classpath:spring/datasource.xml", "classpath:spring/config/packages.xml"})

public class ItemQuantityCartBasicTest extends AbstractTest{
	
	// Service under Test -----------------
	@Autowired
	private ItemQuantityCartService itemQuantityCart;
	
	// Helper functions -------------------
	private ItemQuantityCart generateTestItemQuantityCart()
	{
		ItemQuantityCart ItemQuantityCart = new ItemQuantityCart();
		
		Money money = new Money();
		money.setAmount(50);
		
		Tax tax = new Tax();
		tax.setAmount(21);
		
		//Category category = new Category();
		//category.setAdditivity(true);
		
		Item item = new Item();
		//item.setCategory(category);
		item.setDeleted(false);
		item.setDescription("description_test");
		item.setItemStockInWarehouse(new ArrayList<ItemStockInWarehouse>());
		item.setName("itemName_test");
		item.setNumSold(20);
		item.setPicture("picture_test");
		item.setPrice(money);
		item.setSku("sku_test");
		item.setTags(new ArrayList<String>());
		item.setTax(tax);
		
		
		ItemQuantityCart.setItem(item);	
		ItemQuantityCart.setItemQuantity(12);
		ItemQuantityCart.setShoppingCart(new ShoppingCart());
		
		
		
		
		return ItemQuantityCart;
	}

	@Test
	public void test_createItemQuantityCart(){
		authenticate("admin1");
		itemQuantityCart.save(generateTestItemQuantityCart());
		authenticate(null);
		
	}
}
